<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="@color/mainGreen"
    tools:context=".ExpenseReceiptActivity" >

    <TextView
        android:id="@+id/receiptTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_alignParentTop="true"
        android:text="Receipt"
        android:textColor="@color/mainYellow"
        android:textSize="6mm" />

    <Button
        android:id="@+id/reciptSubmitButton"
        android:layout_width="wrap_content"
        android:layout_height="10mm"
        android:layout_alignLeft="@+id/receiptTitle"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:background="@color/mainYellow"
        android:text="Submit Receipt"
        android:textColor="@color/mainGreen" />

    <ImageView
        android:id="@+id/receiptView"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:layout_above="@+id/reciptSubmitButton"
        android:layout_alignLeft="@+id/receiptTitle"
        android:layout_alignRight="@+id/reciptSubmitButton"
        android:layout_below="@+id/receiptTitle"
        android:scaleType="center"
        android:src="@android:drawable/ic_menu_camera" />

</RelativeLayout>
